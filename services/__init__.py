# This file makes the "services" folder a Python package.
# Without this __init__.py, Python would not recognize "services" as a package,
# and you couldn't do imports like: from services.gemini_processor import process_image
